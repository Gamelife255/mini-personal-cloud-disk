# 代办事项

## 功能需求

- 用户注册、登录、身份校验（JWT）（√）
- 文件上传、下载、删除、重命名(暂时不需要)（√）
- 新建文件夹、文件移入/移出目录 (√)
- 图片视频在线预览、普通文件下载、psd文件预览、文本文件在线查看、音乐文件在线播放
- 个人文件隔离，只能查看自己上传资源
- 文件列表分页展示、按时间排序、搜索文件名、图片类型文件按照颜色分类展示
- 用户修改账户密码，账号找回功能
- 自定义云盘存储路径
- 自定义左侧菜单选项，如：图片、文档、视频、音乐（缺少）等分类展示

## 安全性（优先级从高到低）

- 校验真实文件类型，防止恶意文件上传
- 密码 BCrypt 加密存库
- JWT 登录鉴权（√）
- 上传文件类型白名单 + 重命名
- 每个文件绑定 userId，接口权限校验
- 过滤路径遍历字符
- 文件访问权限控制，确保用户只能访问自己的文件(√)
- 防止 SQL 注入和 XSS 攻击（√）
- 文件下载接口增加访问频率限制，防止暴力破解
- 定期清理过期的文件和无效数据，减少安全风险
- 定期更新依赖库，修复已知安全漏洞
- 代码审查和安全测试，及时发现和修复潜在的安全问题
- 用户注册验证邮箱，防止垃圾账号注册
- 文件上传大小限制，防止恶意占用存储空间

## 个性化

- 图片类型文件按照颜色分类展示
- 用户自定义头像
- 文件列表支持按文件类型、大小、修改时间等多维度排序（√）
- 文件搜索支持模糊匹配和正则表达式
- 文件预览支持更多格式，如视频文件、office文档等（√）
- 文件分享功能，生成分享链接，设置访问权限和有效期
- 文件版本管理，支持文件历史版本查看和恢复
- 文件批量操作，如批量下载、删除、移动等（√）
- 用户空间使用情况统计和展示
- 文件回收站功能，误删文件可恢复
- web页面自定义背景（二次元必备）

## 暂时存在的问题

- 在前端创建文件夹，但在本地文件夹里没有创建，导致文件无法上传和移动（√）
- Redis 存储验证码（更专业）
- 加入邮箱验证功能，用户注册时发送验证码邮件，找回密码时发送验证码邮件（√）
- 邮箱验证功能存在问题，可以正常接收到注册的验证码，也可以通过验证码成功注册账号，但是前端会提示发送失败.（√）
- 启动脚本问题

## 文件回收站功能详细

- 用户删除文件时，文件不会立即从服务器删除，而是被移动到回收站目录中。
- 回收站中的文件会保留一定的时间
- 保留时间由用户自定义，用户可以选择保留时间的长短，例如7天、30天、90天等。
- 在此期间用户可以随时恢复被删除的文件。
- 用户可以在回收站中查看已删除的文件列表，并选择恢复或永久删除文件。
- 需要一个前端设置页面来允许用户设置回收站的保留时间，并显示当前回收站中的文件数量和占用空间。
- 用户的设置应该保存在数据库中（建立相关表）

## 管理员账户（√）

- 管理员账户具有更高的权限，可以管理所有用户的文件和账户。
- 管理员可以查看所有用户的文件列表，进行文件管理操作，如删除、移动、重命名等。
- 管理员可以管理用户账户，如禁用账户、重置密码等。
- 个人网盘一般没有多个账户存在，加入管理员以备遇到同一计算机多个用户使用的情况

## 跨计算机使用

- 用户可以在不同计算机上登录同一账户，访问和管理自己的文件。
- 用户的文件存储在服务器上，不受登录设备的限制。
- 网盘肯定需要跨设备访问才有意义
- 文件存储在用户的个人pc或者nas之类的设备上面，访问设备只需要通过web端对文件进行访问
- 在不同网络下也能进行访问

## 对图像文件的颜色分类展示（待实现）

- 对于上传的图像文件，系统会分析图像的主色调，并根据颜色将图像分类展示。
- 用户可以在文件列表中选择按颜色分类展示图像文件，例如红色、蓝色、绿色等。
- 颜色分类展示可以帮助用户更快速地找到特定颜色的图像文件
- 需要使用图像处理库（如 OpenCV）来分析图像的主色调，并将颜色信息存储在数据库中。
- 前端需要实现颜色分类展示的界面，允许用户选择颜色分类进行过滤展示。
- 对应psd文件的颜色分类展示也需要分析psd文件中的图层颜色信息，并进行分类展示。
- 但面对sai2，csp等其他绘图软件的文件，可能无法分析颜色信息，因此只能对常见的图像格式（如jpg、png）进行颜色分类展示。

## 文件备注功能

- 用户可以为每个文件添加备注信息，方便自己记忆文件内容或用途。
- 备注信息可以在文件列表中显示，用户可以快速查看和编辑备注信息。
- 需要在数据库中为文件表添加备注字段，并提供接口支持备注信息的添加、修改和删除。
- 前端需要在文件详情页或文件列表中提供备注信息的输入框和显示区域，允许用户编辑和查看备注信息。

## web页面自定义背景（√）

- 用户可以在个人设置中上传自定义背景图片，作为网盘界面的背景
- 用户可以选择使用默认背景或自定义背景，系统会根据用户的选择显示相应的背景图片
- 需要在数据库中为用户表添加背景图片字段，并提供接口支持背景图片的上传和设置。
- 前端需要在个人设置页面提供背景图片的上传功能，并在网盘界面根据用户的设置显示相应的背景图片。
- 用户上传的背景图片需要进行安全检查，防止恶意文件上传，同时限制背景图片的大小和格式，以确保界面加载性能和安全性。
- 可以内置几款默认背景供用户选择，满足不同用户的个性化需求。（传教）

## 预览功能的完善

- 图片预览时的窗口可以自由拖动调整大小，支持全屏预览

## 下载功能的完善

- 单个文件下载时，可以自定义文件名，避免下载后文件名过于混乱

## 图表功能的完善

- 个人空间使用情况统计图表，展示用户的存储空间使用情况、文件类型分布等信息，帮助用户更好地管理自己的文件和空间。
- 文件访问统计图表，展示用户的文件访问频率、下载次数等信息，帮助用户了解哪些文件更受欢迎或需要清理。
- 文件上传统计图表，展示用户的文件上传趋势、每天上传的文件数量等信息，帮助用户了解自己的文件上传习惯和趋势。
- 文件类型分布图表，展示用户的文件类型分布情况，例如图片、文档、视频等，帮助用户更好地管理和分类自己的文件。

## 其他功能
-把云盘功能淡化
-加入一些其他功能，增加用户粘性，例如在线画板、在线文本编辑器、在线音乐播放器等
-在线画板功能，用户可以在云盘界面直接使用在线画板工具
-社交功能，用户可以关注其他用户，查看他们的公开文件和动态，增加社交互动
-文件标签功能，用户可以为文件添加标签，方便分类和搜索
-文件分享功能，用户可以生成文件的分享链接，设置访问权限和有效期
-聊天功能，用户可以在云盘界面与其他用户进行实时聊天，分享文件和交流
-加个单独的页面，放小霸王模拟器，让用户可以在线玩一些经典的游戏，增加娱乐性

## 性能优化
- 文件列表分页加载，避免一次性加载过多文件导致页面卡顿  
- 文件预览时使用缩略图，减少加载时间和带宽消耗
- 文件上传和下载时使用分块传输，提升大文件的传输效率
- 使用缓存机制，减少重复的文件访问和计算，提高响应速度

## 社交功能 
- 用户可以关注其他用户，查看他们的公开文件和动态，增加社交互动
- 动态功能，用户可以发布动态，分享自己的文件和生活点滴，其他用户可以点赞和评论
- 消息通知功能，用户可以收到关注者的动态更新、评论回复等通知，保持社交互动的及时性
- 用户可以创建和加入兴趣小组，分享和讨论特定主题的文件和内容，增加社交互动的深度