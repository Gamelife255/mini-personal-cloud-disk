<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.disk.mapper.LikeMapper">

    <insert id="insert" parameterType="com.disk.entity.Like" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO forum_likes (topic_id, user_id, created_at) VALUES (#{topicId}, #{userId}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM forum_likes WHERE topic_id = #{topicId} AND user_id = #{userId}
    </delete>

    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM forum_likes WHERE topic_id = #{topicId} AND user_id = #{userId}
    </select>

    <select id="countByTopicId" resultType="int">
        SELECT COUNT(*) FROM forum_likes WHERE topic_id = #{topicId}
    </select>

</mapper>
